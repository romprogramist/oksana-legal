# Content Updates — Batch 1

**Date:** 2026-04-15
**Scope:** 12 точечных правок по содержимому и структуре сайта + две новые секции + замена floating-кнопки + подготовка 3 PDF-файлов оферты.
**Not in scope:** Изменение `<title>`/meta-описаний; дублирование дисклеймера «Банкротство влечёт…»; вопрос «оставить только QR-код оплаты» (п.13 — отложен); UI-ссылки на 3 PDF-оферты (размещение отложено).

---

## 1. Изменения в константах (`src/lib/constants.ts`)

### 1.1 CONTACT
```ts
workHours: "Пн-Пт: 10:00-19:00",  // было "Пн-Пт: 9:00-18:00"
```
`workHoursExtra` не меняем (`"Сб: по записи"`).

### 1.2 STATS
Удалить 4-й элемент (`"от 15 мин / бесплатная консультация"`). Во 2-м элементе `"15+"` → `"10+"`.

Итоговый массив:
```ts
export const STATS = [
  { value: "500+", label: "успешных дел" },
  { value: "10+", label: "лет опыта" },
  { value: "98%", label: "довольных клиентов" },
] as const;
```

### 1.3 FAQ_ITEMS

**Вопрос о единственном жилье** — новый ответ:
```
Нет, ваше единственное жильё сохраняется, а также вы имеете право сохранить ипотечное жильё, если оно является вашим единственным жильём.
```

**Вопрос о работе во время процедуры** — обрезать после «образ жизни.»:
```
Да, банкротство не влияет на трудоустройство. Вы можете продолжать работать, получать зарплату и вести обычный образ жизни.
```

### 1.4 NAV_LINKS
Добавить 2 пункта; финальный порядок:
```ts
export const NAV_LINKS = [
  { href: "#hero", label: "Главная" },
  { href: "#services", label: "Услуги" },
  { href: "#pricing", label: "Цены" },
  { href: "/payment-methods", label: "Оплата" },
  { href: "#how-we-work", label: "Как работаем" },
  { href: "#faq", label: "FAQ" },
  { href: "#useful", label: "Полезное" },
  { href: "#contact", label: "Контакты" },
] as const;
```

### 1.5 Новый массив USEFUL_DOCS
```ts
export type UsefulDoc = { title: string; description: string; file: string };
export const USEFUL_DOCS: UsefulDoc[] = [];
```
Пустой на старте — секция рендерит плейсхолдер «Скоро здесь появятся образцы документов». Не `as const`, чтобы тип массива оставался изменяемым для будущих добавлений.

---

## 2. Точечные правки компонентов

### 2.1 `Header.tsx`
- `"Юрист Подопригора"` → `"Оксана Юрьевна"`.
- Поддержка path-ссылок в nav: `NAV_LINKS.map` уже рендерит `<a href={link.href}>`, path-ссылки работают нативно. Никаких доп. условий не нужно — браузер сделает полный переход.

### 2.2 `Footer.tsx`
- Лого `"ЮП"` → `"ОЮ"`.
- Описание под лого: `"Профессиональная юридическая помощь… Более 15 лет опыта в сфере банкротства."` → `"Профессиональная юридическая помощь в решении финансовых проблем. Более 10 лет опыта в сфере банкротства."`.
- В блоке «Контакты» удалить строку с WhatsApp, заменить на МАКС:
  ```tsx
  <li><a href={CONTACT.max} className="...">МАКС</a></li>
  ```
- Добавить в `CONTACT`:
  ```ts
  max: "https://max.ru/+79289077260",
  ```

### 2.3 `WhyUsSection.tsx`
- `"Более 15 лет мы специализируемся"` → `"Более 10 лет мы специализируемся"`.

### 2.4 `ContactSection.tsx`
- Изменений не требуется — `CONTACT.workHours` подтягивается автоматически.

---

## 3. Замена floating-кнопки

### 3.1 Удалить `FloatingWhatsApp.tsx`
Файл полностью удалить.

### 3.2 Создать `FloatingMax.tsx`
- Фиксированная круглая кнопка 56×56 в правом нижнем углу.
- Фоновый градиент: `bg-gradient-to-br from-[#8B5CF6] to-[#6D28D9]` (фиолетовый, как запросил пользователь).
- Иконка: `<Image src="/icons/max.svg" width={28} height={28} alt="" />`.
- Ссылка: `https://max.ru/+79289077260` (из `CONTACT.max`).
- `target="_blank" rel="noopener noreferrer"`.
- `aria-label="Написать в МАКС"`.
- Hover: `scale-110` (тот же подход, что у старой кнопки).
- Плавная тень: `shadow-lg hover:shadow-2xl hover:shadow-purple-500/30 transition-all`.

### 3.3 Обновить `page.tsx`
- Заменить импорт `FloatingWhatsApp` → `FloatingMax` и соответствующий тег.

### 3.4 Скачать иконку
- Источник: `https://upload.wikimedia.org/wikipedia/commons/9/9d/Max_%28app%29_logo.svg` (public domain).
- Путь назначения: `public/icons/max.svg`.
- Создать директорию `public/icons/` если не существует.

---

## 4. Новая секция «Полезное»

### 4.1 Компонент `src/components/UsefulSection.tsx`
- `<section id="useful" className="section-padding">`.
- Заголовок: «Полезное», подзаголовок: «Образцы документов для скачивания».
- Если `USEFUL_DOCS.length === 0` → рендерить плейсхолдер: центрированный текст «Скоро здесь появятся полезные образцы документов».
- Если не пусто → сетка карточек:
  - `grid md:grid-cols-2 lg:grid-cols-3 gap-4`
  - Каждая карточка: `bg-white rounded-2xl shadow-soft p-6` с иконкой `<FileText />`, title, description и кнопкой «Скачать» (`<a href={doc.file} download>`).
- Использовать `AnimatedSection animation="fade-up"` вокруг контента (как в других секциях).

### 4.2 Включить в `page.tsx`
Между `TestimonialsSection` и `ContactSection`:
```tsx
<TestimonialsSection />
<UsefulSection />
<ContactSection />
```

---

## 5. Подготовка 3 PDF оферт

### 5.1 Источники (docx)
- `C:\Users\Roman\Downloads\Telegram Desktop\Проект_Договор_оферта_Банкротство (2).docx`
- `C:\Users\Roman\Downloads\Telegram Desktop\Проект_Договор_оферта_гражданские_дела (2).docx`
- `C:\Users\Roman\Downloads\Telegram Desktop\Проект_Договор_оферта_на_повышение_кредитного_рейтинга (2).docx`

### 5.2 Назначение (pdf)
Создать директорию `public/docs/offers/` и положить туда с упрощёнными ASCII-именами:
- `offer-bankruptcy.pdf`
- `offer-civil.pdf`
- `offer-credit-rating.pdf`

### 5.3 Метод конвертации
Приоритет:
1. **LibreOffice headless**: `soffice --headless --convert-to pdf --outdir <dst> <src>` — если установлен.
2. **Python `docx2pdf`** (через установленный MS Word) — если п.1 недоступен.
3. **Pandoc** (требует LaTeX) — резерв.

Проверить наличие инструмента перед началом; если ни один не доступен — остановиться и сообщить пользователю.

### 5.4 UI-размещение
**Не добавляется в этой итерации.** Пользователь отложил вопрос о том, где показывать ссылки (на `/offer`, в футере, или в отдельной секции). Файлы просто лежат в `public/docs/offers/` и доступны по прямым ссылкам, если кто-то их знает. Ссылок в UI нет.

---

## 6. Success criteria

- [ ] `constants.ts` обновлён (workHours, STATS, FAQ_ITEMS×2, NAV_LINKS, USEFUL_DOCS, CONTACT.max).
- [ ] `Header.tsx` рендерит «Оксана Юрьевна».
- [ ] `Footer.tsx` рендерит «ОЮ», «Более 10 лет…», показывает МАКС вместо WhatsApp.
- [ ] `WhyUsSection.tsx` содержит «Более 10 лет».
- [ ] Hero показывает 3 бейджа статистики вместо 4.
- [ ] В nav видны 8 пунктов с «Оплата» и «Полезное».
- [ ] Клик по «Оплата» ведёт на `/payment-methods`.
- [ ] Клик по «Полезное» скроллит на `#useful`.
- [ ] `FloatingWhatsApp.tsx` удалён; `FloatingMax.tsx` создан; иконка скачана в `public/icons/max.svg`.
- [ ] Секция «Полезное» рендерится с плейсхолдером.
- [ ] 3 PDF файла лежат в `public/docs/offers/`.
- [ ] TypeScript проходит `tsc --noEmit` без ошибок.
- [ ] Dev-сервер подхватывает изменения, страница `/` отдаёт 200 с брендом «Оксана Юрьевна».

---

## 7. Out of scope (явно)

- Изменение `<title>` и `<meta description>` — не трогаем.
- Дублирование «Банкротство влечёт…» выше по странице — уже в футере, не дублируем.
- Вопрос 13 (оставить только QR-оплату) — отложен.
- Размещение UI-ссылок на 3 PDF-оферты — отложено.
- Анимация хедера (сделано в прошлой итерации) — не трогаем.
- Редизайн /payment-methods — не трогаем.
